void GetColorBasedOnPercentage(float4 color, float3 position , float topval, float middleval, out float4 newColor)
{
	newColor = color;
}

void Average_float(float X, float Y, out float Avg)
{
	Avg = (X + Y) / 2;
}
